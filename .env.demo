# =============================================================================
# Nightingale Demo Environment Configuration
# =============================================================================
# Pre-configured for demo with cloud Supabase.
# Copy this file to .env to run the demo.
#
# IMPORTANT: These credentials are for demo purposes only.
# The collab server features (real-time collaboration) require additional
# secrets not included here. The app will gracefully fall back to "Local Only"
# mode, which is fully functional for demo purposes.

# I will provide you with the secrets in a separate expiring 1Password link, you
# can use that to populate the values here for a local demo, or refer to the vercel
# link for the live demo.
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (Demo Project)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Alias for services that expect SUPABASE_URL without NEXT_PUBLIC prefix
SUPABASE_URL=

# -----------------------------------------------------------------------------
# Hocuspocus Collaboration Server
# -----------------------------------------------------------------------------
# Note: Real-time collaboration requires JWT_SECRET and SERVICE_ROLE_KEY
# which are not included in demo mode. The app will show "Local Only" status
# and function normally - edits save directly to Supabase.
HOCUSPOCUS_PORT=1234

# -----------------------------------------------------------------------------
# AI Service (Groq)
# -----------------------------------------------------------------------------
GROQ_API_KEY=
AI_SERVICE_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# General
# -----------------------------------------------------------------------------
NODE_ENV=development

# -----------------------------------------------------------------------------
# Demo User Credentials
# -----------------------------------------------------------------------------
# Nightingale Family Clinic users:
TEST_CLINICIAN_EMAIL=
TEST_CLINICIAN_PASSWORD=
TEST_STAFF_EMAIL=
TEST_STAFF_PASSWORD=
TEST_PATIENT_EMAIL=
TEST_PATIENT_PASSWORD=
TEST_ADMIN_EMAIL=
TEST_ADMIN_PASSWORD=
